SET( SRC_FILES Logger.cpp  main.cpp  Map.cpp  MySQL.cpp  Server.cpp)
SET ( CMAKE_BUILD_TYPE Debug )
SET ( CMAKE_VERBOSE_MAKEFILE true )
add_executable(${CMAKE_PROJECT_NAME}.out ${SRC_FILES})


target_link_libraries(${CMAKE_PROJECT_NAME}.out 
    # for spdlog
    PUBLIC spdlog::spdlog
)
target_include_directories(${CMAKE_PROJECT_NAME}.out
    PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include
    # for spdlog
#    PUBLIC ${CMAKE_SOURCE_DIR}/lib/spdlog/include
    PUBLIC ${SPDLOG_PATH}
    # for rapidjson
#    PUBLIC ${CMAKE_SOURCE_DIR}/lib/rapidjson/include
    PUBLIC ${RAPIDJSON_PATH}
)
if (${SPDLOG_FOUND})    
endif()

add_dependencies(${CMAKE_PROJECT_NAME}.out ${CMAKE_SOURCE_DIR}/lib)
